# Required — your Anthropic API key for Claude CLI
ANTHROPIC_API_KEY=sk-ant-...

# Optional — GitHub personal access token for pushing repos
GITHUB_TOKEN=ghp_...

# Optional — override the data directory (default: ./data)
# DATA_DIR=/data

# Optional — server port (default: 3000)
# PORT=4000
